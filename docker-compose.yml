
version: "3.1"

services: 
  mysql:
    image: mysql:latest
    container_name: mysql-server
    environment:
      - MYSQL_ROOT_PASSWORD=elcuervopetshop
    ports:
      - "3333:3306"
    volumes:
      - ./ELCUERVO-CAPITANELLI3/mysql:/estructura.sql